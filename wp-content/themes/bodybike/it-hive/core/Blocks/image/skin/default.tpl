<div class="row item">
    {{label}}
    <div class="image">{{image}}</div>
    <input type="hidden" id="{{id}}" name="{{name}}" value="{{value}}" />
    <div class="btn-controls">
        <a href="#" class="delete-img">{{textRemove}}</a>
        <a href="#" class="upload-img">{{textUpload}}</a>
    </div>
</div>